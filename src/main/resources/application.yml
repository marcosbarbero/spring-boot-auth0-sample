spring:
  security:
    oauth2:
      client:
        registration:
          auth0:
            clientId: your-client-id
            clientSecret: your-client-secret
            scope:
              - openid
              - profile
              - email
        provider:
          auth0:
            issuer-uri: https://marcosbarbero.eu.auth0.com/